# 共通ルール

README.mdがあれば、それを一回参照してリポジトリの情報を得ること。

ユーザーが「やって」「直して」など明示的に実装を指示した場合のみ、実装を行う。
それ以外は質問として解釈し、調査・回答すること。
ただし、実コードを修正するかわりに具体的な修正案を提示することは積極的にやること。

ユーザーの指示に曖昧な箇所がある場合、自分がそれを何だと解釈したのかを行動前に報告すること。

逆に、steer時は停止を指示されたとき以外停止せず続行すること。

コード修正案を表示するときは、修正先のパスと行数を常に近くに書くこと。

質問への回答で、「条件を変える・絞るならYES」のときは「基本的にYES」ではなく「基本的にNO」という方向で答えること。

複数の方針・思考で迷う場合、概要とメリット・デメリットをユーザーに提示すること。

調査・検討で複数個を列挙するときは、連番の番号を付けること。

ファイルパス・リンクは前後にスペースを空けて日本語と間を離す。 `xxx/yyy` のように表示すること。

## コマンド

コマンドを提示する場合、「上から順に実行して、何も問題が起こらない」順序で提示すること。

実行前に確認しておく必要がある条件・選択肢・注意事項は、コマンドより前に提示すること。

上から実行した結果、他コマンドが終わった後で、同じ効果を出せくなるコマンドは、他のコマンドより先に提示すること。

## GitHub

`gh`および`curl`ではなく、GitHub MCPを使用すること。
