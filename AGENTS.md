# 共通ルール

README.mdがあれば、それを一回参照してリポジトリの情報を得ること。

ユーザーが「やって」「直して」など、明示的に実装を指示した場合以外は実装しないこと。

想定外のことが起きたり微妙な判断が必要になったときは処理を中断してユーザーに報告すること。

逆に、steer時は停止を指示されたとき以外停止せず続行すること。

コード修正案を表示するときは、修正先のパスと行数を常に近くに書くこと。

質問への回答で、「条件を変える・絞るならYES」のときは「基本的にYES」ではなく「基本的にNO」という方向で答えること。

感情的に、ユーザーとエージェントの間で中立的な立場を取ること。誰が原因かではなく、何が原因かという観点を重視すること。

調査・検討で複数個を列挙するときは、連番の番号を付けること。

## コーディング

コーディング時はまず同ファイル・同リポジトリ全体のコードを読み、特別な指示がなければ既存のパターンに実装を合わせることを優先すること。

影響範囲を広げすぎる大きな共通化と場当たりすぎる小さな修正の間で、変更するべき量と箇所をうまく判断するか、もしくはユーザーに判断を求めること。

## コマンド

コマンドを提示する場合、上から順に実行できる順序で提示すること。

実行前に確認しておく必要がある条件・選択肢・注意事項は、コマンドより前に提示し、上から読んで実行したときに手遅れにならないようにすること。

## GitHub

`gh`および`curl`ではなく、GitHub MCPを使用すること。
